[project]
name = "startup-validator"
version = "0.1.0"
description = "OpenEnv AI startup validator"
dependencies = [
"flask",
"openenv-core>=0.2.0",
"openai",
"python-dotenv"
]

[project.scripts]
server = "server.app:app"
